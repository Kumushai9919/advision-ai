# Face Recognition Worker Requirements

# Broker client library (local package)
# This provides MessageProducer and MessageConsumer
# Note: Will be installed from ../broker in Docker build

# Core messaging (also in broker, but needed for worker)
pika==1.3.2

# Environment management
python-dotenv==1.0.0

# HTTP requests for API data loading
requests==2.31.0

# Core Python packages (usually included, but explicit for clarity)
typing-extensions==4.8.0

# Optional: for more advanced logging
colorlog==6.7.0

# Face Processing Dependencies
opencv-python==4.8.1.78
numpy==1.26.4
Pillow==10.0.1
scikit-learn==1.3.0
insightface==0.7.3
onnxruntime>=1.17.0,<2.0
# pip install onnxruntime-gpu==1.20.1 #GPU-CUDA usage

# Testing (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0
